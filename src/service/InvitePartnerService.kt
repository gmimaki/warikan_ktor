package com.example.service

class InvitePartnerService {
    /*
    fun findUserByInviteToken(email: String, password: String): User? {
        return  transaction {
            User.find { Users.email.eq(email) and Users.password.eq(password)}.singleOrNull()
        }
    }
     */
}